# Supabase project (client-side; the anon key is public by design)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# GEMINI_API_KEY is NOT set here — it lives as a Supabase Edge Function
# secret (supabase secrets set GEMINI_API_KEY=...) and never ships to the client.
